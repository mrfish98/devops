name: Hello world
on:
  push:
    branches: [main]
jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: build fe
        run: |
          echo "step ls"
          ls
          echo "step pwd"
          pwd
      