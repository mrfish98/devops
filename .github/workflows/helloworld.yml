name: Hello world
on:
  push:
    branches: [main]
jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: build fe
        run: |
          echo "step pwd"
          pwd
      - name: ls
        run: ls -la
      